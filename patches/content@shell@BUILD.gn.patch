diff --git a/content/shell/BUILD.gn b/content/shell/BUILD.gn
index 09637c7a1cea..74ed97274137 100644
--- a/content/shell/BUILD.gn
+++ b/content/shell/BUILD.gn
@@ -75,7 +75,6 @@ static_library("content_shell_app") {
   ]
   deps = [
     ":content_shell_lib",
-    "//components/crash/core/common:crash_key",
     "//content/public/app",
     "//v8",
   ]
@@ -86,12 +85,6 @@ static_library("content_shell_app") {
       "//content/web_test:web_test_renderer",
     ]
   }
-  if (!is_fuchsia) {
-    deps += [
-      "//components/crash/core/app",
-      "//components/crash/core/app:test_support",
-    ]
-  }
   if (is_mac) {
     sources += [
       "app/paths_mac.h",
@@ -267,10 +260,6 @@ static_library("content_shell_lib") {
   if (is_fuchsia) {
     deps += [ "//third_party/fuchsia-sdk/sdk/fidl/fuchsia.ui.policy" ]
   } else {
-    deps += [
-      "//components/crash/content/browser",
-      "//components/crash/core/app",
-    ]
   }
 
   if (enable_plugins) {
@@ -590,9 +579,9 @@ if (is_mac) {
 
   bundle_data("content_shell_framework_helpers") {
     testonly = true
-    sources = [ "$root_out_dir/chrome_crashpad_handler" ]
+    sources = []
     outputs = [ "{{bundle_contents_dir}}/Helpers/{{source_file_part}}" ]
-    public_deps = [ "//components/crash/core/app:chrome_crashpad_handler" ]
+    public_deps = []
     foreach(helper_params, content_mac_helpers) {
       sources += [
         "$root_out_dir/${content_shell_helper_name}${helper_params[2]}.app",
@@ -801,12 +790,6 @@ group("content_shell_crash_test") {
       mac_bin_path + "otool",
     ]
   }
-  if (is_posix) {
-    data += [
-      "//components/crash/content/tools/generate_breakpad_symbols.py",
-      "//components/crash/content/tools/dmp2minidump.py",
-    ]
-  }
   if (is_win) {
     data_deps += [ "//build/win:copy_cdb_to_output" ]
   }
