diff --git a/headless/BUILD.gn b/headless/BUILD.gn
index d2ab76aed326..633e9257cc09 100644
--- a/headless/BUILD.gn
+++ b/headless/BUILD.gn
@@ -275,13 +275,6 @@ source_set("headless_shared_sources") {
   sources += generated_devtools_api
   sources += get_target_outputs(":protocol_sources")
 
-  if (!is_fuchsia) {
-    sources += [
-      "lib/headless_crash_reporter_client.cc",
-      "lib/headless_crash_reporter_client.h",
-    ]
-  }
-
   deps = [
     ":gen_devtools_client_api",
     ":protocol_sources",
@@ -295,13 +288,6 @@ source_set("headless_shared_sources") {
     "//url",
   ]
 
-  if (!is_fuchsia) {
-    deps += [ "//components/crash/content/browser" ]
-  }
-  if (is_component_build && is_win) {
-    deps += [ "//components/crash/core/app:crash_export_thunks" ]
-  }
-
   configs += [ ":inside_headless_component" ]
 }
 
@@ -439,7 +425,6 @@ component("headless_non_renderer") {
     }
 
     deps += [
-      "//components/crash/core/common:crash_key",
       "//components/security_state/content",
       "//gin",
       "//third_party/blink/public:blink",
@@ -448,13 +433,6 @@ component("headless_non_renderer") {
       "//v8",
     ]
 
-    if (!is_fuchsia) {
-      deps += [ "//components/crash/content/browser" ]
-    }
-    if (is_win) {
-      deps += [ "//components/crash/core/app:crash_export_thunks" ]
-    }
-
     if (enable_basic_printing) {
       deps += [
         "//components/printing/browser",
@@ -480,7 +458,6 @@ component("headless_non_renderer") {
   }
 
   if (is_mac) {
-    deps += [ ":mac_helpers" ]
     frameworks = [
       "AppKit.framework",
       "CoreFoundation.framework",
@@ -545,7 +522,6 @@ if (!is_component_build) {
 
     deps = [
       ":headless",
-      "//components/crash/core/common:crash_key",
       "//third_party/blink/public:blink_headers",
       "//ui/base",
       "//v8",
@@ -590,10 +566,6 @@ test("headless_unittests") {
     "//testing/gtest",
   ]
 
-  if (is_win) {
-    deps += [ "//components/crash/core/app:crash_export_thunks" ]
-  }
-
   if (enable_basic_printing) {
     sources += [ "lib/browser/headless_printing_unittest.cc" ]
     deps += [
@@ -603,16 +575,6 @@ test("headless_unittests") {
   }
 }
 
-if (is_mac) {
-  copy("mac_helpers") {
-    sources = [ "$root_out_dir/chrome_crashpad_handler" ]
-
-    deps = [ "//components/crash/core/app:chrome_crashpad_handler" ]
-
-    outputs = [ "$root_out_dir/Helpers/{{source_file_part}}" ]
-  }
-}
-
 test("headless_browsertests") {
   configs += [ "//net:net_test_config" ]
   sources = [
@@ -645,10 +607,6 @@ test("headless_browsertests") {
     "test/data/",
   ]
 
-  if (is_mac) {
-    data_deps = [ ":mac_helpers" ]
-  }
-
   defines = [ "HAS_OUT_OF_PROC_TEST_RUNNER" ]
 
   deps = [
@@ -732,10 +690,6 @@ static_library("headless_shell_lib") {
     public_deps += [ ":headless_non_renderer" ]
   }
 
-  if (!is_fuchsia) {
-    deps += [ "//components/crash/content/browser" ]
-  }
-
   if (enable_basic_printing) {
     deps += [
       "//components/printing/browser",
@@ -749,8 +703,6 @@ static_library("headless_shell_lib") {
     defines = [ "HEADLESS_USE_CRASHPAD" ]
 
     deps += [
-      "//components/crash/core/app:crash_export_thunks",
-      "//components/crash/core/app:run_as_crashpad_handler",
       "//content:sandbox_helper_win",
       "//sandbox",
     ]
